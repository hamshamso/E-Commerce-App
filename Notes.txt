HTTP request compose of 
1. method   :   (POST/GET/UPDATE/DELETE)
2. URL      :   (DOMAIN/PATH/QUERY PARAMS)
3. Headers  :   ("Content-Type" : "application/json" ,
                "Authorization":`Bearer ${token}`)
4. Body     :   (email,password ...)

and we can add new properties like 'user' in validatUser 
composition of decoded : _id,iat,exp
<input
        type="email"
        name="email" // Matches key in formData and req.body
        value={formData.email}
        onChange={handleChange}
/>
what i learned from this project :
1. Orgenize your folders/files
2. Use async/error hundler to save time
3. Host your database in your PC
4. Don't pile up ( لا تكدس) your files/function in one parent  